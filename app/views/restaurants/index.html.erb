<h1>Restaurants index</h1>
<ul>
  <% @restaurants.each do |restaurant| %>
    <li>Name: <%=restaurant.name%> (<%= link_to 'details', restaurant_path(restaurant) %>)</li>
  <% end %>
</ul>

<p><%= link_to 'Add a new restaurant', new_restaurant_path %></p>
